{
  "name": "apixelocean.github.io",
  "version": "1.0.0",
  "description": "A Pixel Ocean Website",
  "main": "js/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server --port=8000 --no-browser",
    "compile-less": "lessc css/style.less css/style.css",
    "start": "concurrently \"npm run compile-less\" \"npm run serve\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "tailwindcss": "^3.4.12"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
