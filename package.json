{
  "name": "apixelocean.github.io",
  "version": "1.0.0",
  "description": "A Pixel Ocean Website",
  "main": "js/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server --port=8000 --no-browser",
    "build-css": "tailwindcss build -i css/index.css -o css/main.css",
    "start": "concurrently \"npm run build-css\" \"npm run serve\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "tailwindcss": "^3.4.12"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
